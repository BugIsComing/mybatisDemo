<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--引入属性配置文件-->
    <properties resource="application.properties" />
    <!--查看官网如何设置日志,在maven中引入了log4j后不需要设置setting，只需要配置log4j.properties即可-->
    <!--http://www.mybatis.org/mybatis-3/zh/logging.html-->

    <!-- 取别名 -->
    <typeAliases>
        <typeAlias  type="com.lc.model.User" alias="user"/>
    </typeAliases>

    <!--<environments default="development">-->
        <!--<environment id="development">-->
            <!--<transactionManager type="JDBC" />-->
            <!--&lt;!&ndash; 配置数据库连接信息 &ndash;&gt;-->
            <!--<dataSource type="POOLED">-->
                <!--<property name="driver" value="com.mysql.jdbc.Driver" />-->
                <!--<property name="url" value="jdbc:mysql://localhost:3306/lctest" />-->
                <!--<property name="username" value="root" />-->
                <!--&lt;!&ndash;<property name="password" value="xxx" />&ndash;&gt;-->
                <!--<property name="password" value="123456" />-->
            <!--</dataSource>-->
        <!--</environment>-->
    <!--</environments>-->

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <property name="driver" value="${db.driver}" />
                <property name="url" value="${db.url}" />
                <property name="username" value="${db.username}" />
                <property name="password" value="${db.password}" />
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mapper/userMapper.xml"/>
    </mappers>

</configuration>